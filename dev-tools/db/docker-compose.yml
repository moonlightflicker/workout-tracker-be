version: '3.9'

name: workout-tracker-db

services:
  mongodb:
    image: mongo:5.0
    ports:
      - 27017:27017
    volumes:
      - ~/apps/mongo:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=workout_tracker
      - MONGO_INITDB_ROOT_PASSWORD=secret